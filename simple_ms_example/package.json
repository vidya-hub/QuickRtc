{
  "name": "simple-mediasoup-example",
  "version": "1.0.0",
  "description": "Complete example using Simple MediaSoup client and server libraries",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npm run build:example && nodemon dist/server.js",
    "start:https": "npm run build:example && USE_HTTPS=true nodemon dist/server.js",
    "dev": "node --loader ts-node/esm src/server.ts",
    "dev:https": "USE_HTTPS=true node --loader ts-node/esm src/server.ts",
    "watch": "nodemon --exec \"node --loader ts-node/esm\" src/server.ts",
    "watch:https": "USE_HTTPS=true nodemon --exec \"node --loader ts-node/esm\" src/server.ts",
    "generate-certs": "mkdir -p certs && openssl req -x509 -newkey rsa:4096 -keyout certs/key.pem -out certs/cert.pem -sha256 -days 365 -nodes -subj '/C=US/ST=CA/L=San Francisco/O=Simple MediaSoup/OU=Development/CN=localhost'",
    "test-https": "./test-https-setup.sh",
    "setup": "npm install && npm run build && npm run generate-certs",
    "refresh-deps": "./refresh-dependencies.sh",
    "refresh-deps:node": "node refresh-dependencies.js",
    "build": "npm run build:client && npm run build:server && npm run build:types && npm run build:example",
    "build:example": "tsc",
    "build:types": "cd ../simple_ms_types && npm run build",
    "build:server": "cd ../simple_ms_server && npm run build",
    "build:client": "cd ../simple_ms_client && npm run build",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "mediasoup",
    "webrtc",
    "video-conference",
    "real-time",
    "streaming"
  ],
  "author": "Simple MediaSoup",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.5",
    "simple_ms_server": "file:../simple_ms_server",
    "simple_ms_types": "file:../simple_ms_types",
    "cors": "^2.8.5",
    "mediasoup": "^3.19.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "@types/cors": "^2.8.17",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.1",
    "nodemon": "^3.0.2"
  }
}
